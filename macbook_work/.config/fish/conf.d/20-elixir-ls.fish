# Adds local Elixir-LS to the PATH
# NOTE: The standard name of the start script is `start_languageserver.sh`
# it must be renamed in its folder so it can be found under the right name
fish_add_path $HOME/elixir-ls/release/
